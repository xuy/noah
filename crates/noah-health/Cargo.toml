[package]
name = "noah-health"
version = "0.1.0"
edition = "2021"
description = "Health score types and scoring logic for Noah"

[dependencies]
serde = { version = "1", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }
